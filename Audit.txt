Audit the ENTIRE MEDILINK codebase to identify what is currently DEMO / MOCK / SIMULATED / PARTIAL / REAL / BROKEN / MISSING across the complete emergency pipeline.

IMPORTANT: DO NOT modify, delete, create, refactor, or overwrite ANY files. ONLY inspect and report.

FIRST — REPOSITORY DISCOVERY

First inspect the repository structure and identify the actual frontend, backend, ML/model, Android, database, and configuration directories/files before auditing.

Do not assume folder names such as "backend/", "frontend/", "model/", etc.

TARGET ARCHITECTURE

Real ESP32 Sensors → BLE → Flutter → FastAPI → Real ML Model → Risk Detection → Emergency Event → Patient Confirmation/Escalation → Emergency Contact → Native Android Phone Call

---

REPORT SIZE LIMIT

Keep the final report concise and actionable — maximum ~1,500 words.

- Prioritize blockers.
- Do not list every minor file.
- Include only relevant evidence.
- Maximum 5 files per component.
- Group duplicate findings.
- Working components can be summarized in one line.
- Do not provide replacement code.

EVIDENCE & CONFIDENCE RULES

For every major finding provide:

STATUS: REAL / PARTIAL / DEMO / MOCK / MISSING / BROKEN
CONFIDENCE: HIGH / MEDIUM / LOW / UNKNOWN
EVIDENCE: exact file + line number/function when available.

Never guess. If evidence is insufficient, explicitly say:

INSUFFICIENT EVIDENCE

Clearly distinguish between:

- implemented
- configured
- connected
- actually executed/used
- simulated/demo behavior

---

1. EXECUTIVE SUMMARY

Report:

- Overall system status
- Approximate completion percentage
- Top 5 blockers
- Top 5 most important files/functions

Use:

Component| Status| Confidence| Key Evidence
Real Vitals| | | 
BLE| | | 
AI/ML| | | 
Emergency Trigger| | | 
Emergency Event| | | 
Emergency Contact| | | 
Native Calling| | | 
Repeat Emergencies| | | 

---

2. VITALS + BLE AUDIT

Search the entire repository for:

"demo" "mock" "dummy" "simulate" "simulation" "random" "heartRate" "spo2" "temperature" "bloodPressure" "BLE" "Bluetooth" "flutter_blue" "flutter_blue_plus" "ESP32"

Determine:

- Where vitals actually originate.
- Whether ESP32 sensor code exists.
- Whether real sensor readings are produced.
- Whether BLE is actually implemented.
- Whether Flutter receives real BLE data.
- Whether Flutter sends those values to FastAPI.
- Whether FastAPI receives real values.
- Whether database records are real or seeded/demo data.
- Whether any random/hard-coded vitals remain in the active pipeline.

Output only the important findings:

File:Line | Function | Status | Confidence | Evidence | Required Change

---

3. AI / ML AUDIT

Determine:

- Whether an actual trained ML model exists.
- Model type.
- Training/inference files.
- Model loading path.
- Feature inputs.
- Prediction/inference function.
- Whether the model is actually called.
- Whether predictions use live vitals.
- Whether thresholds are hard-coded.
- Whether random/demo risk values are used.
- Whether the frontend displays actual backend predictions.

Search for:

"risk" "predict" "model" "joblib" "pickle" "RandomForest" "XGBoost" "classification" "critical" "warning" "normal" "threshold"

Clearly distinguish:

trained model exists ≠ model is actually connected to live vitals.

Output key findings with evidence.

---

4. EMERGENCY / SOS AUDIT

Trace the complete flow:

Vitals → AI Risk → Emergency Trigger → Emergency Event → Confirmation → Escalation

Check:

- Manual SOS implementation.
- AI-based emergency trigger.
- Emergency event creation.
- Unique event ID generation.
- Database persistence.
- Confirmation state.
- Escalation state.
- Cancellation.
- Resolution.
- Emergency state machine.
- "hasCalled" or similar permanent flags.
- Cooldowns.
- Global/static variables.
- Event-specific call state.
- Why the current demo call may work only once.
- Whether every NEW emergency event can initiate a new call.

Determine whether the current implementation is event-based or incorrectly session/global-state based.

---

5. NATIVE CALLING AUDIT

Trace:

Emergency → Patient → Emergency Contact → Phone Number → Native Android Call

Search for:

"call" "phone" "tel:" "url_launcher" "CALL_PHONE" "permission_handler" "Twilio" "makeCall" "initiateCall"

Determine:

- Whether calling is demo or real.
- Whether "url_launcher" or equivalent is used.
- Whether Android "CALL_PHONE" permission exists.
- Whether runtime permission is requested.
- Whether the actual native phone capability is invoked.
- Whether the correct logged-in patient's emergency contact is used.
- Whether phone numbers are hard-coded.
- Whether another/demo user is being used.
- Whether Twilio is still involved.
- Whether the call is tied to the emergency event.
- Whether repeated/new emergencies can trigger new calls.

Output:

File:Line | Function | Status | Confidence | Evidence | Required Change

---

6. AUTH + CONTACT + DATABASE AUDIT

Verify this complete chain:

Login → Current User ID → Patient → Emergency Contact → Emergency Event → Phone Call

Check for:

- Hard-coded user IDs.
- Hard-coded phone numbers.
- Demo accounts.
- Fake patient data.
- Authentication bypasses.
- Incorrect role handling.
- Missing user ID after session restoration.
- Emergency-contact persistence.
- Correct MongoDB document relationship.
- Flutter ↔ FastAPI ↔ MongoDB consistency.
- Whether the currently logged-in patient is always used.

---

7. END-TO-END STATUS

Evaluate the current pipeline:

Vitals → AI → Emergency → Contact → Call

Then evaluate the required real pipeline:

ESP32 → BLE → Flutter → FastAPI → ML → Emergency → Confirmation/Escalation → Contact → Native Call

For every stage provide:

STATUS + CONFIDENCE + one-line evidence

Use only:

REAL / PARTIAL / DEMO / MOCK / MISSING / BROKEN

---

8. PRIORITY CHANGES

Give:

P0 — MUST FIX

Maximum 5 items.

For each:

File → Function → Problem → Evidence → Required Change

P1 — SHOULD FIX

Maximum 5 items.

Use the same format.

Prioritize issues that prevent the system from becoming a genuinely real end-to-end emergency system.

---

9. FINAL VERDICT

Give YES / NO / PARTIAL / UNKNOWN + confidence for each:

- Real wearable vitals?
- Real BLE?
- Real AI model?
- AI connected to live vitals?
- Real emergency event?
- Correct logged-in patient?
- Correct emergency contact?
- Real native phone call?
- Repeat emergency calls?
- Full pipeline without demo/mock data?
- Production-ready?

---

10. MINIMUM PATH TO REAL SYSTEM

Give the shortest practical implementation path:

1. ESP32 real sensors
2. BLE transmission
3. Flutter real vitals reception
4. FastAPI live-vitals ingestion
5. Real ML inference
6. Emergency event creation
7. Patient confirmation/escalation
8. Emergency contact retrieval
9. Native Android phone call
10. Repeat-emergency/event testing
11. Complete end-to-end validation

Mention only the key files/components that need changes.

---

FINAL RULES

DO NOT MODIFY THE CODEBASE.

DO NOT GUESS.

EVIDENCE OVER ASSUMPTIONS.

Do not provide replacement code.

Maximum final report: ~1,500 words.

The goal is to determine exactly what is currently demo and what must change to make MEDILINK a real end-to-end system.